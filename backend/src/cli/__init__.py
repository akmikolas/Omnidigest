"""
CLI Commands package for the OmniDigest Management tool.
OmniDigest 管理工具的 CLI 命令包。

This package contains submodules for each command domain (db, rss, jobs, auth).
Each submodule provides handler functions that are dispatched by `manage.py`.
此包含按命令领域划分的子模块（db、rss、jobs、auth）。
每个子模块提供由 `manage.py` 调度的处理函数。
"""
