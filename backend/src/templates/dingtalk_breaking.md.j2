{% if is_update %}📢 **NEWS UPDATE / 新闻更新** 📢{% else %}🔴 **BREAKING NEWS / 突发新闻** 🔴{% endif %}

**{% if story.source_url %}[{{ story.story_title }}]({{ story.source_url }}){% else %}{{ story.story_title }}{% endif %}**

&nbsp;

⚠️ **等级**: 重要性 {{ story.peak_score }}

📅 **时间**: {{ story.updated_at.strftime('%Y-%m-%d %H:%M:%S UTC') if story.updated_at else 'N/A' }}

📌 **分类**: {{ story.category }}

📡 **信息源**: {{ story.source_count }} 个独立源

&nbsp;

**核心概要**:

{{ story.story_summary }}

{% if events and events|length > 1 %}

&nbsp;

**📰 时间线**:
{% for ev in events[-5:] %}
- {{ ev.created_at.strftime('%Y-%m-%d %H:%M') if ev.created_at else '' }} {% if ev.source_url %}[{{ ev.event_title }}]({{ ev.source_url }}){% else %}{{ ev.event_title }}{% endif %}
{% endfor %}
{% endif %}

---
*由 OmniDigest 突发新闻引擎自动生成*

*Generated by OmniDigest Breaking Engine*
