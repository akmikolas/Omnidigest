{% if is_update %}<b>📢 NEWS UPDATE / 新闻更新 📢</b>
{% else %}<b>🔴 BREAKING NEWS / 突发新闻 🔴</b>
{% endif %}━━━━━━━━━━━━━━━━━━━━━━━

<b>{% if story.source_url %}<a href="{{ story.source_url }}">{{ story.story_title }}</a>{% else %}{{ story.story_title }}{% endif %}</b>

<b>📅 {{ story.updated_at.strftime('%Y-%m-%d %H:%M:%S UTC') if story.updated_at else 'N/A' }}</b>
⚠️ <b>等级</b>: 级别 {{ story.peak_score }}
📌 <b>分类</b>: {{ story.category }}
📡 <b>信息源</b>: {{ story.source_count }} 个独立源

<b>核心概要</b>:

{{ story.story_summary }}

{% if events and events|length > 1 %}
<b>📰 时间线</b>:
{% for ev in events[-5:] %}
• <i>{{ ev.created_at.strftime('%Y-%m-%d %H:%M') if ev.created_at else '' }}</i> {% if ev.source_url %}<a href="{{ ev.source_url }}">{{ ev.event_title }}</a>{% else %}{{ ev.event_title }}{% endif %}
{%- endfor %}
{% endif %}
━━━━━━━━━━━━━━━━━━━━━━━
<i>由 OmniDigest 突发新闻引擎自动生成</i>
<i>Generated by OmniDigest Breaking Engine</i>
