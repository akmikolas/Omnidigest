{% if is_update %}📢 **NEWS UPDATE**{% else %}🔴 **BREAKING NEWS**{% endif %}

---

## {{ story.story_title }}

{% if story.source_url %}[查看原文]({{ story.source_url }}){% endif %}

---

⚠️ **重要性等级**: {{ story.peak_score }}

📅 **时间**: {{ story.updated_at.strftime('%Y-%m-%d %H:%M') if story.updated_at else 'N/A' }}

📌 **分类**: {{ story.category }}

📡 **信息源**: {{ story.source_count }} 个独立源

---

📋 **核心概要**

{{ story.story_summary }}

{% if events and events|length > 1 %}
---

🕐 **时间线** (最近 {{ events|length }} 条)

{% for ev in events[-5:] %}
• {{ ev.created_at.strftime('%Y-%m-%d %H:%M') if ev.created_at else '' }} {% if ev.source_url %}[{{ ev.event_title }}]({{ ev.source_url }}){% else %}{{ ev.event_title }}{% endif %}
{% endfor %}
{% endif %}

---

_由 OmniDigest 突发新闻引擎自动生成_
